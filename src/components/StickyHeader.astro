---
interface Props {
  title: string;
  date?: Date;
}

const { title, date } = Astro.props;
---

<div id="content_header" class="hidden px-4 md:px-6 lg:px-8 py-2 fixed w-full z-10 top-0 left-0 shadow-lg"
  style="background: color-mix(in srgb, var(--color-bg) 95%, transparent); backdrop-filter: blur(12px); border-bottom: 1px solid var(--color-border);">
  <div class="max-w-3xl mx-auto flex items-center gap-4">
    <div class="w-1.5 h-1.5 rounded-full shrink-0" style="background: var(--color-accent);"></div>
    <span class="font-semibold text-sm truncate" style="color: var(--color-text);">{title}</span>
    {
      date && (
        <div class="ml-auto flex items-center gap-2 shrink-0">
          <span class="text-xs" style="color: var(--color-text-muted);">
            {date.toISOString().slice(0, 10)}
          </span>
        </div>
      )
    }
  </div>
</div>
